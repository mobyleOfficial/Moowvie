name: common
description: Platform-aware common UI components for Moovie
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any
  cached_network_image: any
  share_plus: ^11.0.0
  movies_domain:
    path: ../../features/movies/domain
  core:
    path: ../../core
  auth:
    path: ../../features/auth
  get_it: ^8.0.3
  auto_route: ^11.1.0
