name: user_activities_data
description: Data layer for the User Activities feature
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  objectbox: ^5.3.1
  core:
    path: ../../../core
  movies_domain:
    path: ../../movies/domain
  movies_data:
    path: ../../movies/data
  user_activities_domain:
    path: ../domain
