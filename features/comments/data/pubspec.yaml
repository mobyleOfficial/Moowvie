name: comments_data
description: Data layer for the Comments feature
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  comments_domain:
    path: ../domain
  injectable: any
  core:
    path: ../../../core

dev_dependencies:
  build_runner: ^2.13.1
